# 正式域名，例如 https://example.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Preview 保持 preview；正式发布前确认内容后改为 production
NEXT_PUBLIC_SITE_ENV=preview

# 紧急回退开关：设为 false 后重新构建，即可只保留静态 PES poster
NEXT_PUBLIC_ENABLE_PES_3D=true
